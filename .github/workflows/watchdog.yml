name: watchdog
on:
  schedule:
    - cron: "7,37 * * * *"
  workflow_dispatch:
permissions: {}
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - run: python3 watchdog.py
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
